{
  "name": "@bmail/admin",
  "version": "0.1.0",
  "private": true,
  "description": "Operating the BMail platform as a library: Maddy mailboxes over SSH, SES identities, Route 53 zones and the DNS record sets a customer pastes.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
