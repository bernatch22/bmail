{
  // @bmail/db — depends only on @bmail/contract (types) plus runtime deps.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"],
  "references": [
    { "path": "../contract" }
  ]
}
