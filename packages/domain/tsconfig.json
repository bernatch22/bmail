{
  // @bmail/domain — pure logic; depends only on @bmail/contract (types).
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"],
  "references": [
    { "path": "../contract" }
  ]
}
