{
  "name": "@bmail/infra",
  "version": "0.1.0",
  "private": true,
  "description": "bmail infrastructure operations as a library: Maddy mailboxes over gcloud ssh, SES identities, Route 53, client DNS record sets",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
