{
  // @bmail/engine — depends on contract (types) and db (local mail store).
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"],
  "references": [
    { "path": "../contract" },
    { "path": "../db" }
  ]
}
