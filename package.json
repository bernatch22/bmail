{
  "name": "bmail",
  "private": true,
  "version": "0.0.0",
  "description": "BMail monorepo: the mail engine, the client SDK, React components, the admin library and the apps on top",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "dev": "echo 'TODO: wire per-app dev scripts (step 5+)' && exit 1",
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "rollup": "^4.63.0",
    "rollup-plugin-dts": "^6.5.1",
    "typescript": "^5.5.0"
  }
}
