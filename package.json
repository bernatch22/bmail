{
  "name": "bmail",
  "private": true,
  "version": "0.0.0",
  "description": "BMail monorepo: shared contract, domain logic, db, engine, infra and apps",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "dev": "echo 'TODO: wire per-app dev scripts (step 5+)' && exit 1",
    "test": "echo 'TODO: wire test runner once packages have tests' && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
